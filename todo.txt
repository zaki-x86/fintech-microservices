- security
     security properties (auth service, user service) from one place
     write login date to db
     issue method logic
     send request to user service if needed
     grpc
     test if needed
- login api test
- config-server integration
- kibana
- add retry to all microservices while send request
- mock-processing
- mock-billing
- security



   payments
---------------------------
       id
  created_at
  update_at
  transaction_id (billing)
  values (user input)
  user_id
  merchant_id

  billing_check_request
  billing_check_response
  billing_check_status

-----------------------------------------
payment_transaction
-----------------------------------------
id
created_at
update_at
check_merchant_id
provider_id
amount

is_notification_sent

billing_pay_request
billing_pay_response
billing_pay_status

provider_status
provider_request
provider_response
